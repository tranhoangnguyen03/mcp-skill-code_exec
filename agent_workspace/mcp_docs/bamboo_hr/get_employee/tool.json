{
  "name": "get_employee",
  "description": "Fetch a single employee by numeric id.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "employee_id": { "type": "integer", "description": "Employee id." }
    },
    "required": ["employee_id"],
    "additionalProperties": false
  },
  "outputSchema": {
    "oneOf": [
      { "$ref": "#/$defs/Employee" },
      { "type": "null" }
    ],
    "$defs": {
      "Employee": {
        "type": "object",
        "properties": {
          "id": { "type": "integer" },
          "name": { "type": "string" },
          "dept": { "type": "string" },
          "role": { "type": "string" },
          "status": { "type": "string" },
          "hire_date": { "type": "string", "description": "YYYY-MM-DD" },
          "manager": { "type": "string" },
          "slack_id": { "type": "string" },
          "manager_slack_id": { "type": "string" }
        },
        "required": ["id", "name", "dept", "role", "status", "hire_date", "manager", "slack_id", "manager_slack_id"],
        "additionalProperties": false
      }
    }
  },
  "errors": []
}

