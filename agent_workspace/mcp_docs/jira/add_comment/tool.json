{
  "name": "add_comment",
  "description": "Add a comment to an existing ticket.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ticket_id": { "type": "string" },
      "comment": { "type": "string" }
    },
    "required": ["ticket_id", "comment"],
    "additionalProperties": false
  },
  "outputSchema": { "type": "null" },
  "errors": [
    {
      "type": "ValueError",
      "when": "ticket_id does not exist"
    }
  ]
}

